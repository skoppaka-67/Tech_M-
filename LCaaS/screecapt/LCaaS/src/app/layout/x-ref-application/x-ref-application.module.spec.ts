import { XrefApplicationModule } from './x-ref-application.module';

describe('XrefApplicationModule', () => {
    let XrefApplicationModule: XrefApplicationModule;

    beforeEach(() => {
        //XrefModule = new XrefModule();
    });

    it('should create an instance', () => {
        expect(XrefApplicationModule).toBeTruthy();
    });
});
