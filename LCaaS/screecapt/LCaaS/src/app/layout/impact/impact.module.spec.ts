import { ImpactModule } from './impact.module';

describe('ImpactModule', () => {
    let ImpactModule: ImpactModule;

    beforeEach(() => {
        //ImpactModule = new ImpactModule();
    });

    it('should create an instance', () => {
        expect(ImpactModule).toBeTruthy();
    });
});
