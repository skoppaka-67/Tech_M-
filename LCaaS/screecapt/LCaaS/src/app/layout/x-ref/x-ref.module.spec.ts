import { XrefModule } from './x-ref.module';

describe('XrefModule', () => {
    let XrefModule: XrefModule;

    beforeEach(() => {
        //XrefModule = new XrefModule();
    });

    it('should create an instance', () => {
        expect(XrefModule).toBeTruthy();
    });
});
